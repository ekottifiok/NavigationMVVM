<UserControl x:Class="AmazingNavigation2.MVVM.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" Background="#252525"
             d:DesignHeight="300" d:DesignWidth="300">
    
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock Text="Which device are you using to connect?" 
                   FontSize="15" Foreground="White" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Client" CommandParameter="Client" 
                    Command="{Binding NavigateToSettingCommand}" />
            <Button Content="Server" CommandParameter="Server" 
                    Command="{Binding NavigateToSettingCommand}" />
        </StackPanel>
    </StackPanel>
    
</UserControl>
